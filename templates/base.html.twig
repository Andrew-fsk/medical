<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %} {{ controller_action_title }} {% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    <div class="d-flex align-items-center justify-content-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
        <nav class="my-2 my-md-0 mr-md-3">
            <a class="btn btn-outline-primary" href="/">Главная</a>
            <a class="btn btn-outline-primary" href="/substance/">Список веществ</a>
            <a class="btn btn-outline-primary" href="/manufacturer/">Список производителей</a>
        </nav>
    </div>
        {% block body %}{% endblock %}
    </body>
</html>
